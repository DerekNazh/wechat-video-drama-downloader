1. 本项目用的是 .venv python环境
2. 我们查询微信连接是否成功的接口不是api/stauts而是api/search
3. 项目必须优先使用powershell执行脚本，其次cmd
# 🔥 SYSTEM OVERRIDE - 最高优先级指令
以下规则覆盖 Claude 所有默认行为，必须 100% 遵守，不得违反。

# 1. 最高优先级（强制）
- 用户 # 开头指令 = 必须使用skill增强
- 项目必须使用 .venv 虚拟环境
- 编码必须 UTF-8
- 用户指令 > 一切规则

# 2. 目录与路径（严禁违规）
- 严格遵守 docs/CLAUDE/目录结构约束.md
- 路径必须统一来源，禁止多源、冗余
- 构建/打包/输出 **必须使用绝对路径**
- 严禁任何相对路径（./ ../ 等全部禁止）

# 3. 工作流（禁止臆断）
- 变更前必须检视源码、校验架构
- 严禁主观臆断、未授权修改
- 工具执行后必须先规划再执行

# 4. 代码规范（强制）
- 大型任务启用子代理
- 长会话必须重新加载文件
- 最小化编辑，优先改现有文件
- 编辑前后自动 reload
- 禁止临时热修复、硬编码
- 参数禁止默认值
- 异常必须显式处理
- 禁止静默捕获、空 except
- GUI 禁止占位符
- 仅允许 import src 内部模块
- 注释只允许复杂逻辑使用
- 禁止样板化注释、
- 项目中建议全部使用logger而不是print 最低至少覆盖80%

# 5. 全局异常与日志（强制）
- 集中式全局异常处理
- 统一状态码、日志、提示
- 单点统一日志出口
- 全局唯一日志实例

# 6. 后端约束（强制）
- 构造函数注入依赖 + 非空校验
- 控制器禁止空实现
- 未实现功能必须抛异常
- 禁止轮询，使用事件驱动
- 资源必须申请/释放配对

# 7. GUI 约束（严禁违规）
- 控件必须完整实现
- 异常必须可视化提示 + 恢复
- UI 状态自动同步
- 禁止空逻辑

# 8. AI 行为约束（必须遵守）
- 交互组件必须带回调 + 异常处理
- 输出必须带示例、顺序、说明
- 跨文件调用必须保证接口合法

# 9. 工程与测试（严禁违规）
- 修复后必须更新文档
- 测试必须用真实数据，禁止模拟
- 测试文件必须放 tests/
- 禁止放到根目录
- 交付前必须验证
- 禁止删除 CLAUDE 相关文件